# IMPLEMENTATION PLAN: Живое Городище

## Принципы реализации

- Сначала делаем рабочий вертикальный срез: карта -> карточка -> страница здания -> слот -> mock-донат -> летопись.
- Затем подключаем Supabase, админку и реальные интеграции.
- Визуальный стиль держим близко к `Референс.png`.
- Все данные, которые администратор должен менять, выносим в БД.
- Не строим статичный лендинг вместо платформы.

## Текущий прогресс

- Supabase project подключен, миграция и seed применены.
- Публичная карта, страницы зданий, летопись, форма доната и форма волонтера читают данные через Supabase adapter с seed fallback.
- Админские PATCH routes для зданий, слотов, маркеров, летописи, донатов и волонтеров работают через server-side service role client.
- `POST /api/donations` создает реальный `pending` donation; переход в `paid` проверен через admin route, повторный `paid` идемпотентен.
- `POST /api/volunteer-applications` создает реальную заявку; начисление часов проверено через admin route и не создает дублей для одной заявки.
- Админка настроек умеет хранить ручной платежный режим `tbank_collection_manual` и публичную ссылку на Т-Банк Сборы.
- `.env.example` содержит полный набор server env для будущего подключения ЮKassa.
- `/admin` защищен Supabase Auth, `profiles.role` и bootstrap-настройкой `ADMIN_BOOTSTRAP_EMAILS`; admin API routes также требуют роль до service-role действий.
- Добавлены пользовательские `/auth/login` и `/auth/register`.
- Добавлена админская страница `/admin/users` для назначения статусов `participant`, `moderator`, `admin`; `moderator` не имеет доступа к админке.
- Следующий крупный блок: полноценная ЮKassa-интеграция с create-payment и webhook.

## Этап 0. Подготовка

Результат: проект готов к разработке.

Задачи:

- создать Next.js App Router проект;
- подключить TypeScript;
- подключить Tailwind CSS;
- подключить shadcn/ui;
- подключить lucide-react;
- настроить ESLint и форматирование;
- создать базовую структуру директорий;
- добавить env-шаблон.

Структура:

```txt
app/
components/
features/
lib/
db/
public/
supabase/
```

## Этап 1. Дизайн-система и ассеты

Результат: базовая визуальная система и подготовленные изображения.

Задачи:

- описать токены цветов в Tailwind;
- сделать базовый layout;
- сделать header;
- сделать кнопки в стиле проекта;
- сделать карточку на пергаментном фоне;
- сделать прогресс-бар;
- сделать маркер карты;
- оптимизировать изображения из `assets/Здания`;
- положить web-версии ассетов в `public/assets`.

Компоненты:

- `AppHeader`;
- `ParchmentPanel`;
- `ProgressBar`;
- `StatusBadge`;
- `MapMarker`;
- `BuildingPreviewCard`;
- `DonationSlotCard`;
- `ChronicleItem`;
- `BottomSheet`.

## Этап 2. Seed-данные и локальный прототип

Результат: публичная карта работает без БД на локальных seed-данных.

Задачи:

- создать `lib/seed.ts`;
- добавить MVP-здания:
  - Кузница;
  - Огороды;
  - Курятник;
  - Загон для кур;
- добавить координаты маркеров;
- добавить слоты поддержки;
- добавить демо-донаты;
- добавить демо-летопись.

Цель этапа: быстро получить видимый интерфейс как в референсе.

## Этап 3. Публичная карта

Результат: главная страница с интерактивной картой.

Задачи:

- сверстать главную страницу;
- вывести карту;
- вывести маркеры;
- открыть карточку здания по клику;
- добавить фильтры по зоне;
- добавить фильтры по статусу;
- добавить desktop-раскладку;
- добавить mobile bottom sheet;
- добавить список объектов ниже карты;
- проверить адаптив 360px, 768px, 1440px.

Критерии:

- маркеры не съезжают при масштабировании;
- карточка не перекрывает критичные элементы;
- на телефоне можно выбрать здание и закрыть панель.

## Этап 4. Страницы зданий

Результат: у каждого здания есть отдельная страница.

Задачи:

- создать `/objects/[slug]`;
- вывести галерею;
- вывести описание;
- вывести историческую справку;
- вывести прогресс;
- вывести слоты поддержки;
- вывести летопись по зданию;
- добавить SEO metadata.

## Этап 5. Mock donation flow

Результат: можно имитировать пожертвование без ЮKassa.

Задачи:

- создать форму поддержки;
- валидировать поля;
- создать mock-сценарий успешной оплаты;
- обновлять локальный прогресс;
- добавлять запись в летопись;
- показать страницу успешного результата.

Этот этап нужен для проверки UX до интеграции платежей.

## Этап 6. Supabase schema

Результат: база данных готова.

Статус в текущей разработке: начальная схема и seed подготовлены в `supabase/migrations/0001_initial_schema.sql` и `supabase/seed.sql`.

Задачи:

- создать SQL-миграции;
- создать таблицы;
- создать enum-типы;
- добавить индексы;
- добавить RLS;
- добавить seed;
- настроить Storage buckets:
  - `maps`;
  - `buildings`;
  - `icons`;
  - `reports`;
  - `certificates`.

## Этап 7. Подключение Supabase к публичной части

Результат: публичный сайт читает данные из БД.

Статус в текущей разработке: добавлен `@supabase/supabase-js` adapter с seed fallback. При пустом `.env.local` сайт продолжает работать на локальных данных.

Задачи:

- подключить Supabase client;
- вынести запросы в `lib/data`;
- заменить локальные seed-данные запросами;
- оставить fallback seed только для разработки;
- добавить обработку loading/error/empty states.

## Этап 8. Админка: каркас и роли

Результат: защищенный `/admin`.

Задачи:

- подключить Supabase Auth;
- сделать login;
- сделать проверку ролей на сервере;
- создать admin layout;
- создать navigation;
- закрыть маршруты для неадминов.

## Этап 9. Админка карты

Результат: администратор управляет картой и маркерами.

Задачи:

- список версий карты;
- загрузка карты;
- выбор активной карты;
- редактор маркеров;
- создание маркера кликом;
- drag-and-drop;
- редактирование координат;
- связь маркера со зданием;
- выбор иконки;
- предпросмотр.

## Этап 10. Админка зданий и слотов

Результат: контент поселения управляется без кода.

Задачи:

- CRUD зданий;
- CRUD медиа здания;
- CRUD слотов поддержки;
- управление статусами;
- управление видимостью;
- базовая SEO-настройка;
- запись действий в аудит.

## Этап 11. ЮKassa

Результат: реальные платежи работают корректно.

Задачи:

- создать API route для создания платежа;
- создать donation со статусом `pending`;
- передать metadata в ЮKassa;
- реализовать webhook;
- проверить идемпотентность;
- обновлять donation status;
- создавать chronicle entry;
- логировать ошибки.

Критичное правило: только webhook переводит донат в `paid`.

## Этап 12. Летопись

Результат: публичная и административная летопись.

Задачи:

- публичная `/chronicle`;
- фильтры;
- записи по зданию;
- автоматическое создание записей;
- скрытие записи в админке;
- закрепление записи;
- редактирование текста.

## Этап 13. Волонтеры

Результат: волонтерские заявки проходят полный цикл.

Задачи:

- публичная форма;
- запись в БД;
- админский список;
- смена статуса;
- подтверждение часов;
- начисление баллов;
- запись в летопись.

## Этап 14. Уведомления

Результат: администратор и участник получают уведомления.

Задачи:

- Telegram-уведомление о новом донате;
- Telegram-уведомление о новой заявке;
- email после оплаты;
- email после заявки волонтера;
- логирование ошибок отправки.

## Этап 15. Аналитика и экспорт

Результат: админ видит ключевые метрики.

Задачи:

- KPI-дашборд;
- таблица зданий;
- таблица донатов;
- таблица волонтеров;
- CSV export;
- фильтры;
- пагинация.

## Этап 16. Безопасность и юридические страницы

Результат: проект готов к публичному запуску.

Задачи:

- проверить RLS;
- проверить роли;
- проверить webhook;
- проверить загрузку файлов;
- добавить страницы:
  - политика конфиденциальности;
  - согласие на обработку ПДн;
  - условия пожертвования;
  - контакты и реквизиты;
- добавить admin audit log для критичных действий.

## Этап 17. Тестирование и полировка

Результат: MVP стабилен.

Задачи:

- проверить desktop;
- проверить tablet;
- проверить mobile;
- проверить пустые состояния;
- проверить ошибки платежей;
- проверить повтор webhook;
- проверить CSV;
- проверить SEO;
- проверить Lighthouse;
- проверить изображения.

## Этап 18. Деплой

Результат: проект опубликован.

Задачи:

- создать Supabase project;
- применить миграции;
- загрузить seed;
- настроить Storage;
- настроить Vercel;
- настроить env;
- настроить домен;
- настроить webhook URL;
- пройти smoke test.

## Рекомендуемый порядок разработки

1. Карта на seed-данных.
2. Страницы зданий.
3. Mock-донаты.
4. Supabase schema.
5. Чтение публичных данных из Supabase.
6. Админка зданий.
7. Админка карты.
8. ЮKassa.
9. Летопись.
10. Волонтеры.
11. Аналитика.
12. Безопасность и деплой.
